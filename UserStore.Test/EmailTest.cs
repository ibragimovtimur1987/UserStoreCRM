using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using UserStore.Web.Controllers;

namespace UserStore.Test
{
    [TestClass]
    public class EmailTest
    {
        [TestMethod]
        public void Email()
        {
            RequestController controller = new RequestController();
            controller.Create()
        }
    }
}
